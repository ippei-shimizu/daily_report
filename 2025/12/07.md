## やったこと
- AWS基礎コースのオンボーディング
- AWS にデプロイするアプリケーションをローカル環境で動かしてみる
  - ローカル環境構築の全体像を知る 
  - db-migratorの運用手順について理解する
  - TablePlusをインストールする
  - slack-metrics DBのマイグレーションを実行してテーブルを作成する
  - slack-metrics DBのrestoreを実行して、seedデータを作成する
  - [nice to have] DBマイグレーションの運用方法について理解する

## 今日の学び
- 業務では、Ridgepoleでスキーマ管理をしているので、マイグレーションファイルの扱いが久しぶりで勉強になった。
- READMEに記載してあった、マイグレーションファイル作成の例（create_users）を実行してしまい、マイグレーションが失敗し、dirtyがtrueになっていて、wordspacesテーブルのdescriptionカラム追加のマイグレーションができなかったが、デバックを通して理解を深めることができた。
- Goをローカル環境で開発する時は、ホストマシン環境を使用するケースが多いのを知らなかったので勉強になった。そもそも、動的言語は依存関係が複雑だから、Docker環境を使用することが多いのは納得した。

## 感想
- 仕事や諸々落ち着いてきたので、やっとCloude Praticaに着手できた。
- 明日は、db-migratorのGoのコードリーディングに挑戦する。
- マルチステージビルドの理解を深めたい。
- みなさんの過去の日報を見ることで、優秀な方はどういった視点で学習しているのかがわかり、とても勉強になった。